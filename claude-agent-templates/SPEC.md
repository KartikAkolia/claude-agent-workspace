# SPEC.md

What this project is and how it's built, not how to work on it (see `AGENTS.md`).

## Product Definition

`<one paragraph: what this is and who it's for>`

## Goals / Non-Goals

- Goals: `<...>`
- Non-goals: `<...>`

## Architecture

`<stack, data model, key components, how they connect>`

## Functional Requirements

`<per subsystem>`

## Interfaces / Contracts

`<config schema, APIs, file layout, platform support>`

## Testing & CI

`<what's tested, how, and where CI runs it>`

## Acceptance Criteria / Definition of Done

`<the bar a change has to clear to be considered finished>`
