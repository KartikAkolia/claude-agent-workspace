Read `AGENTS.md` in the repository root for operating instructions before doing anything else.

After finishing any real work in this repo (not a pure read/orientation pass), log it in `docs/gemini/handoff.md`, following the dated-entry convention already used there — that file is yours specifically, kept separate from `docs/handoff.md`, which is Claude Code's own continuity log for this same repo. Read `docs/gemini/handoff.md` too, alongside `AGENTS.md`, before resuming work after a session gap.
